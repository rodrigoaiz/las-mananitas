---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Compartir Debate - Las Mañanitas">
  <main class="py-20 px-4 sm:px-6 lg:px-8">
    <div class="max-w-3xl mx-auto">
      <a
        href="/"
        class="inline-flex items-center gap-2 text-slate-400 hover:text-pink-600 font-bold uppercase tracking-widest text-sm mb-12 transition-colors group"
      >
        <svg
          class="w-4 h-4 transform group-hover:-translate-x-2 transition-transform"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="3"
            d="M11 17l-5-5m0 0l5-5m-5 5h12"></path>
        </svg>
        Volver al Debate
      </a>

      <div class="text-center mb-16">
        <div class="text-8xl mb-8 animate-float">📢</div>
        <h1
          class="text-5xl sm:text-6xl font-black text-slate-900 mb-6 tracking-tight leading-tight"
        >
          ¡Rola el <span
            class="bg-gradient-to-r from-pink-600 via-orange-500 to-yellow-500 bg-clip-text text-transparent"
            >Debate</span
          >!
        </h1>
        <p class="text-xl text-slate-500 font-medium">
          Ayúdanos a llegar a más tíos y primos para que por fin sepamos qué
          versión es la ganadora.
        </p>
      </div>

      <div class="grid gap-6 sm:grid-cols-2">
        <button
          class="bg-[#1DA1F2] text-white p-8 rounded-[2.5rem] font-black text-2xl hover:scale-105 transition-all duration-300 shadow-xl shadow-blue-200/50 flex flex-col items-center gap-4"
          onclick="alert('Compartiendo en Twitter...')"
        >
          <span class="text-4xl">🐦</span>
          Twitter / X
        </button>

        <button
          class="bg-[#25D366] text-white p-8 rounded-[2.5rem] font-black text-2xl hover:scale-105 transition-all duration-300 shadow-xl shadow-green-200/50 flex flex-col items-center gap-4"
          onclick="alert('Compartiendo en WhatsApp...')"
        >
          <span class="text-4xl">💬</span>
          WhatsApp
        </button>

        <button
          class="bg-[#1877F2] text-white p-8 rounded-[2.5rem] font-black text-2xl hover:scale-105 transition-all duration-300 shadow-xl shadow-blue-300/50 flex flex-col items-center gap-4"
          onclick="alert('Compartiendo en Facebook...')"
        >
          <span class="text-4xl">👥</span>
          Facebook
        </button>

        <button
          class="bg-white text-slate-900 p-8 rounded-[2.5rem] font-black text-2xl hover:scale-105 transition-all duration-300 shadow-xl shadow-slate-200/50 border-2 border-slate-100 flex flex-col items-center gap-4"
          onclick="navigator.clipboard.writeText(window.location.origin); alert('¡Enlace copiado al portapapeles!')"
        >
          <span class="text-4xl">🔗</span>
          Copiar Enlace
        </button>
      </div>

      <div
        class="mt-16 bg-pink-50 rounded-[2.5rem] p-10 text-center border-2 border-pink-100"
      >
        <p class="text-pink-600 font-bold text-lg italic">
          "El chisme compartido se disfruta el doble."
        </p>
        <p class="text-pink-400 font-medium mt-2">- El Tío Mañanitas</p>
      </div>
    </div>
  </main>
</Layout>
